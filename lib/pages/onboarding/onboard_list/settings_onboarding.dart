// Settings page of Onboarding
import 'package:flutter/material.dart';

class OnBoardingSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("OnBoarding Settings"),
      ),
    );
  }
}
